// import { createPost, getPosts, getSinglePost } from './posts';

//export { createPost, getPosts, getSinglePost };

import { getProducts } from "./products";
import { login, me, logout } from "./auth";

export { getProducts, login, logout, me };
